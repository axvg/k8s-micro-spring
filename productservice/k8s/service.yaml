apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: product-service
  labels:
    app: product-service

spec:
  type: NodePort

  selector:
    app: product-service

  ports:
    - name: http
      protocol: TCP

      port: 80

      targetPort: 8082

      nodePort: 30082
